export interface State {
  output: string;
  firstNumber: string;
  secondNumber: string;
  operation: string;
  equalOperation: string;
  status: boolean;
  isEqualed: boolean;
  clearSecondNumber: boolean;
  canDelete: boolean;
  disabled: boolean;
}
